let x; // Adicione isso!
let y; // Adicione isso!x = 0;
do {
    x = x + 1;
    if (x == 3) break;
    x;
} while (x < 10);
x;

y = 0;
do {
    y = y + 1;
    if (y == 2) continue;
    y;
} while (y < 5);